- flash.each do |msg_type, message|
  %div{:class => "alert alert-#{msg_type.to_s == 'notice' ? 'success' : 'danger'} alert-dismissible fade show"}
    = message
    %button.btn-close{"aria-label" => "Close", "data-bs-dismiss" => "alert", :type => "button"}
